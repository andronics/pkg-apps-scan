name="scan"
description="Document Scanner - OCR and document digitization"
version="0.1.0"
method="stow"
collection="apps"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=("scanimage")
ignore=(
    "README.md"
)
